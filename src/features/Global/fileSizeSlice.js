import { createSlice } from "@reduxjs/toolkit";

const fileSizeSlice = createSlice({
    name: "fileSize",
    initialState: {
        fileSize: 0,
    },
    reducers: {
        setFileSize: (state, action) => {
            
        }
    }
})